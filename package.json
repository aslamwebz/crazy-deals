{
  "name": "crazydeals",
  "version": "1.0.0",
  "description": "CrazyDeals - E-commerce platform with flash sales",
  "private": true,
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend": "cd backend && php artisan serve --port=8084",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && composer install"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
